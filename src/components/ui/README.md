# shadcn/ui components

This project expects shadcn/ui components to live here, e.g. `button.tsx`, `badge.tsx`, etc.

Quick install (after `npx shadcn@latest init`):

```bash
npx shadcn@latest add button badge card command dialog dropdown-menu input label popover scroll-area separator tabs toggle tooltip resizable table button-group
```
